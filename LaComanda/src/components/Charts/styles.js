
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textTitle: {
    fontSize: 20,
    textAlign: 'center'
  },
  charTitle: {
    textAlign: 'center',
    fontSize: 19
  }
});
export default styles;
